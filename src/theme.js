import { createTheme } from '@mui/material/styles';

const UITheme = createTheme({
    palette: {
        primary: {
            main: '#1976d2',
        },
        secondary: {
            main: '#dc004e',
        },
    },
});

export default UITheme;
